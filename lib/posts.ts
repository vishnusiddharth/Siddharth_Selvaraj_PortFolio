export type Post = {
  title: string;
  excerpt: string;
  date: string;
  readTime: string;
  url: string;
};

/**
 * Empty by design — nothing to cite yet.
 * Add entries here and the Writing section appears automatically.
 */
export const posts: Post[] = [];
